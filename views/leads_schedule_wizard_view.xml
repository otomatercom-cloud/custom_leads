<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_leads_schedule_wizard_form" model="ir.ui.view">
        <field name="name">leads.schedule.wizard.form</field>
        <field name="model">leads.schedule.wizard</field>
        <field name="arch" type="xml">
            <form string="Schedule Zoom/Walk-in">
                <group>
                    <field name="lead_id" invisible="1"/>
                    <field name="schedule_type" widget="radio"/>
                    <field name="schedule_date"/>
                </group>
                <footer>
                    <button name="action_schedule" string="Schedule" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_leads_schedule_wizard" model="ir.actions.act_window">
        <field name="name">Schedule Meeting</field>
        <field name="res_model">leads.schedule.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
